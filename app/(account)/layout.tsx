import { AccountLayout } from '@/components/layouts'
import type { LayoutProps } from '@/types'

export default function __AccountLayout({ children }: LayoutProps) {
  return <AccountLayout>{children}</AccountLayout>
}
